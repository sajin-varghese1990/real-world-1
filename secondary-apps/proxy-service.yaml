apiVersion: v1
kind: Service
metadata:
  name: web-svc          # same namespace as the Ingress
  namespace: shop
spec:
  type: ExternalName
  externalName: web-svc.default.svc.cluster.local   # real service, real namespace
