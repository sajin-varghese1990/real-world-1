apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: shop-base
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sajin-varghese1990/real-world-1.git
    targetRevision: main
    path: k8s/base
  destination:
    server: https://kubernetes.default.svc
    namespace: shop
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
