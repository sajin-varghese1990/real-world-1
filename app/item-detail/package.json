{
  "name": "shop-item-detail",
  "version": "1.0.0",
  "private": true,
  "description": "Per-item detail page (description + icon), served under /item on the same Ingress",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.16.3"
  }
}
